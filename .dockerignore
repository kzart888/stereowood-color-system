# Node / build artifacts
node_modules
npm-debug.log*
Dockerfile*
.dockerignore
.git
.gitignore
*.swp

# SQLite WAL/SHM transient (bind mount may regenerate)
backend/*.db-wal
backend/*.db-shm

# Local uploads already mounted at runtime
backend/uploads/*

# OS / editor
.DS_Store
Thumbs.db
.vscode
